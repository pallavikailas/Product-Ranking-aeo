# Copy this file to .streamlit/secrets.toml and fill in your key for local dev.
# On Streamlit Community Cloud, add GROQ_API_KEY via the dashboard instead
# (App settings → Secrets) — do NOT commit secrets.toml to git.

GROQ_API_KEY = "gsk_..."
